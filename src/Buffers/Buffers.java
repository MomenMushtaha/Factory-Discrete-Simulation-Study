package Buffers;

public class Buffers {
    /*
     * Each component is listed as a method ex. (C11 c12 c22 etc..)
     * Each buffer has a queue that can be represented with a data structure
     * Each buffer will havew a status value to indicate if capacity of 2 is reached 
    */
    
}
